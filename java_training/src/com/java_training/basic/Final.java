package com.java_training.basic;

public class Final {
	// Final keyword provides restriction to the java data members
	//If we are using final keyword with variable then we cannot change the variable its behaving like a constant
	//if we are using final keyword with the method then we cannot override the method.
	//if we are using final keyword with the class we cannot extend the property of the class.
	
}
